# Examples

These files are toy inputs that document expected formats. They are not the full evaluation data and do not include raw annual reports, model outputs, or human labels.

- `companies_sample.csv`: minimal company list format for batch-style scripts.
- `sample_manifest.csv`: minimal filing manifest format for documenting data provenance.

Use your own downloaded public filings for actual extraction runs.

